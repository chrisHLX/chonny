# Hand-verified scalar-field corrections for spells that already have a correct `spells` row
# and correct spell_class_availability (imported via SimC, PvP-talent JSON, or manual-spells.txt)
# but are missing cooldown_seconds/duration_seconds/mechanic because their SOURCE data doesn't
# carry it at all — not an availability problem, a plain missing-fact problem.
#
# Distinct from the other three override files in this folder, on purpose:
#   - baseline-spec-overrides.txt fixes AVAILABILITY (which spec a spec_id=NULL baseline spell
#     belongs to) for a spell that's already otherwise fully described.
#   - manual-spells.txt CREATES a spell + availability from scratch for something missing from
#     SimC entirely.
#   - cc-synergies-overrides.txt is CC-chain-specific fields only (dr_category/chain_target/
#     is_peel/is_interrupt/pvp_duration_seconds), a different feature's data.
#   - This file touches none of the above — it only ever updates cooldown_seconds/
#     duration_seconds/mechanic/description on a spell that ImportSpellData::importScalarCorrections()
#     confirms ALREADY exists for the current patch. If the spell doesn't exist yet, the line is
#     skipped with a warning, never used to create one (that's manual-spells.txt's job).
#
# Format: spell_id | field=value, field=value, ... | note (comment only, not parsed)
# Valid fields: cooldown_seconds, duration_seconds, mechanic (must match a key in
# ModuleSpellReferenceService::MECHANIC_CATEGORY_MAP), spell_type (Melee|Magic|Ranged|None —
# added 2026-08-23 for wow:cc-formula's melee-vs-ranged rule), range_yards (free text, e.g.
# "5 yards" — same addition), cast_type (instant|cast — added 2026-08-23 for spells whose
# cast_type went stale the same way as the "224 spells" gap documented in CLAUDE.md's "cast_type
# parser" section: the spell_id has been repurposed by a later patch's SimC dump for a totally
# different spell, so the main import path can never revisit our own, still-correct row for it).
# Omit a field entirely to leave it alone — this is a field-level patch, not a full record
# replace.
#
# Verify each line the same way as everywhere else in this folder before adding: in-game, a real
# spellbook snapshot's resolved_description, or equally solid evidence — never guessed.

# Scatter Shot (Hunter PvP talent, spell_id 213691) — confirmed absent from SimC's raw dump
# entirely (checked both filtered and raw hunter.txt directly, only ever referenced as a text
# mention inside another spell's DR-style damage-reduction description, never its own record) and
# the addon's collectPvpTalents() never calls a cooldown API at all (id/name/desc only — see
# main.lua), so cooldown/duration/mechanic can never come from any data source already on file.
# duration_seconds + mechanic recovered from the real Hunter snapshot's resolved tooltip text
# (2026-08-13-batch1.lua, snapshot #2): "...incapacitates the target for 3 sec. Any damage caused
# will remove the effect." cooldown_seconds supplied directly by the user from their own research
# (2026-08-14) — not recoverable from any source this project has on file, real-world confirmed
# fact only.
213691 | cooldown_seconds=30, duration_seconds=3, mechanic=Incapacitate | Scatter Shot

# Grapple Weapon (Windwalker Monk PvP talent, spell_id 233759) — confirmed absent from SimC's
# raw dump entirely (checked both filtered/monk and raw/monk.txt directly, zero records for
# this spell_id anywhere), same shape as Scatter Shot above: a PvP-talent-only spell with no
# SimC coverage at all, so nothing already on file could ever have populated these fields.
# duration_seconds=5 confirmed via wow:find-cc-duration (built 2026-08-14 specifically for
# this kind of gap) scanning every stored arena log: 8 real SPELL_AURA_APPLIED->REMOVED
# instances across 4 independent matches, all 5.006-5.036s — matches the ability's own
# tooltip text exactly ("...returning them to you for 5 sec"). cooldown_seconds=45 is a
# WEAKER-confidence inference, not a stated fact: the same scan found a real minimum gap of
# 45.862s between two successive casts by the same player in one match (proof the ability was
# off cooldown by then, never proof of the exact value) — recorded as 45 because it's a
# suspiciously clean number just under that floor and matches common PvP-talent cooldown
# design, not because it was confirmed via tooltip/spellbook/direct source. Flag and correct
# if a harder source (in-game tooltip, a real snapshot) ever contradicts this.
233759 | cooldown_seconds=45, duration_seconds=5 | Grapple Weapon

# Strangulate (Death Knight PvP talent, spell_id 47476) — confirmed absent from SimC's raw dump
# entirely (checked both filtered/deathknight and raw/deathknight.txt directly, zero records for
# this spell_id anywhere) and Blizzard's own PvP-talent JSON never carries a cooldown field on
# any entry (checked data/pvptalents/death-knight.json directly — id/name/spell_id/spell_name/
# description/unlock_player_level/compatible_slots/playable_spec_id/playable_spec_name only), so
# nothing already on file could ever have populated cooldown_seconds. duration_seconds is already
# correctly set via cc-synergies-overrides.txt (pvp_duration_seconds=4, silence). cooldown_seconds
# confirmed 2026-08-18 via a real WoWHead tooltip screenshot supplied by the user: "45 sec
# cooldown" — same confidence tier as Scatter Shot's user-supplied research above.
47476 | cooldown_seconds=45 | Strangulate

# Shockwave (Warrior, spell_id 46968 — the cast/display copy wow:cc-formula's kit-enumeration
# actually shows for Fury Warrior, per CLAUDE.md's Shockwave sibling-fix note) — the raw dump
# has no "Range :" line for this spell_id at all (confirmed 2026-08-23 via direct grep), so
# spell_type/range_yards were both null. Real ability is a frontal cone attack, ~5 yards,
# confirmed directly by the user (2026-08-23): "shockwave a cone shape in front of them maybe
# 5 yards."
46968 | spell_type=Melee, range_yards=5 yards | Shockwave

# Axe Toss (Warlock/Demonology pet command, spell_id 119914 — the cast/display copy
# wow:cc-formula actually shows) — raw dump shows "Range : 50000 yards" on this exact spell_id,
# an obvious placeholder rather than a real distance (confirmed 2026-08-23). Corrected to the
# same real ability's other spell_id copy's own genuine parsed value (89766, "30 yards" — real
# SimC data, not a placeholder), preferred over the user's own recollection ("must be 40 off the
# pet, I'm not sure") since 30 yards comes from actual source data rather than memory, though
# the two aren't far apart and either would tell the same story for wow:cc-formula's purposes.
119914 | range_yards=30 yards | Axe Toss

# "50000 yards" placeholder pattern, found systemically 2026-08-23 while testing wow:cc-formula
# against real comps — a third real hard-CC ability (beyond Shockwave/Axe Toss above) turned up
# with the identical bogus value. All three are genuine melee/close-range abilities in real
# gameplay; the pattern looks like SimC using 50000 as a "not really range-gated the normal way"
# placeholder for certain melee-weapon-range-gated effects rather than a real distance. Audited
# every currently hard-CC-tagged spell for this exact shape (range_yards >= 1000) — these two
# plus Axe Toss above were the only three found.
#
# Garrote - Silence (Rogue/Assassination, spell_id 1330, the real aura spell_id — see CLAUDE.md's
# Garrote fix history) — a stealth backstab opener, corrected to 5 yards matching Cheap Shot's
# own real value (same "must be right on top of the target" mechanic).
1330 | range_yards=5 yards | Garrote - Silence

# Shockwave's OTHER copy (Warrior, spell_id 132168 — the real aura spell_id per CLAUDE.md's
# sibling-fix note; not currently visible to wow:cc-formula's kit-enumeration since only 46968
# above is, but corrected here too for DB consistency in case that ever changes). Same real
# ability, same real ~5 yard cone as 46968 above.
132168 | spell_type=Melee, range_yards=5 yards | Shockwave (real aura copy)

# Rake (Druid/Feral, spell_id 163505 — the confirmed real stealth-opener Stun, see CLAUDE.md's
# Rake dr_category fix history) — a melee bleed/combo-point generator, corrected to 5 yards
# matching every other real stealth-opener's value (Cheap Shot, Garrote above).
163505 | range_yards=5 yards | Rake

# Polymorph (Mage, spell_id 118) — confirmed 2026-08-23 that this spell_id has been repurposed
# in the current SimC dump for an unrelated Warlock/Destruction spell (grepped raw/mage.txt and
# filtered/mage/*.txt directly: no "Name : Polymorph" record exists anywhere under this patch,
# only "Mass Polymorph" and description mentions) — same "went stale, main import path can't
# revisit it" gap CLAUDE.md's cast_type-parser section already flagged for ~224 spells
# dataset-wide, spotted here specifically while investigating why wow:cc-formula's --no-real-data
# fallback scoring treated Polymorph as worse than even a cast-time ability (missing cast_type
# defaults to the worst tier). Our own row 118 is still the correct, live, currently-displayed
# Polymorph (already has verified_override rows for all 3 Mage specs, confirmed rendering
# correctly on the real site) — only its scalar fields went stale. cast_type=cast is real-world
# knowledge (Polymorph has always had a real cast bar, ~1.5s, a stable fact across the game's
# history), not derived from any file.
118 | cast_type=cast | Polymorph

# Chimaeral Sting (Hunter PvP talent, spell_id 356719, all 3 specs — Beast Mastery/Marksmanship/
# Survival) — same shape as Scatter Shot above: cooldown never present in any source this project
# has on file (zero spell_effects rows at all for this spell_id, confirmed directly). Its own
# description IS fully captured (a real 3-stage venom chain: Scorpid Venom 90% slow 3s -> Spider
# Venom Silence 3s -> Viper Venom 20% dmg/heal reduction 3s), just with no cooldown component in
# that text — cooldown supplied directly by the user from their own play knowledge (2026-08-27),
# real-world confirmed fact, not recoverable from any imported source.
356719 | cooldown_seconds=60 | Chimaeral Sting
