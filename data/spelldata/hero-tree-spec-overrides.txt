# Hand-verified corrections to hero-tree -> spec associations, applied by
# ImportSpellData::scanHeroTreeSpecs() AFTER it parses each hero-*.txt file's own
# "(SpecA, SpecB) [tree=hero, ...]" tag. Same "flag, don't guess" precedent as
# baseline-spec-overrides.txt / cc-synergies-overrides.txt: this file exists because the
# UPSTREAM SimC dump text itself is wrong for the affected entries below, not because our
# own parser (scanHeroTreeSpecs()) misread it — verify by hand before adding a line, add
# one line at a time, never bulk-derive.
#
# Root cause, found 2026-08-15 investigating a user report that the site's Havoc/Vengeance/
# Devourer hero-tree associations looked wrong: SimC's own raw dump files
# (data/spelldata/filtered/demonhunter/hero-*.txt) literally tag Fel-Scarred and Void-Scarred
# as "(Havoc, Devourer)" and Annihilator as "(Generic)" (not a real spec name at all, which
# is why Annihilator ended up with zero spec links before this file existed — silently
# skipped by scanHeroTreeSpecs()'s spec-name lookup). Confirmed directly with the user
# (who has real, current game knowledge — Devourer is brand-new content, plausibly why
# SimC's community-maintained dump hasn't caught up yet): the real pairing is
# Havoc/Vengeance sharing Fel-Scarred + Aldrachi Reaver (same as their live-game precedent),
# with Devourer (a new 3rd spec) getting its own exclusive pair, Annihilator + Void-Scarred.
# Aldrachi Reaver's own dump tag ("(Havoc, Vengeance)") was already correct and needs no
# override — not listed below, matching this file's "only list actual corrections" convention.
#
# Format: classSlug | heroTreeNameSlug | comma-separated spec names | note
# heroTreeNameSlug must match scanHeroTreeSpecs()'s own normalizeSlug() of the hero tree's
# display name (e.g. "Fel-Scarred" -> "fel-scarred") — verify against a real import run's
# output if a future addition doesn't seem to take effect.

demonhunter | fel-scarred | Havoc,Vengeance | SimC dump says (Havoc, Devourer) - wrong, confirmed by user 2026-08-15
demonhunter | void-scarred | Devourer | SimC dump says (Havoc, Devourer) - Devourer-exclusive per user confirmation 2026-08-15
demonhunter | annihilator | Devourer | SimC dump says (Generic), not a real spec name - Devourer-exclusive per user confirmation 2026-08-15
