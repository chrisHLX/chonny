# Hand-authored spells missing from SimulationCraft's own raw dump entirely — confirmed absent
# at the source (not our own fetch/filter pipeline dropping them), so there is no spell record
# for the importer to annotate the way baseline-spec-overrides.txt annotates an existing one.
# SimC has no reason to track a spell with zero combat-simulation relevance (pure incapacitate,
# no damage/healing/resource component) — Polymorph is the confirmed case that started this file
# (2026-08-08); Hex is NOT one of these (it exists in SimC's dump as "Hex (desc=Frog)", excluded
# by the unrelated `(desc=...)` display filter — a different, smaller fix, not a manual-spells
# case). Repentance and Scatter Shot are correctly absent right now (pruned from the live game
# this patch; Scatter Shot confirmed returning 12.1) — do not add them here.
#
# Re-checked 2026-08-13 against this session's SimC pull (labeled 12.1-era content, see
# CLAUDE.md's patch-refresh notes) — Scatter Shot is STILL absent, same as before (only
# referenced in another spell's description prose, no "Name:" record of its own, and not present
# in the Blizzard talent-tree data either). This contradicts a separate report (from a different
# machine's own diff-spellbook run) that it "was in the raw files just not pulled" — not yet
# reconciled; flagging rather than guessing which is right. Do not add it here until that's
# resolved — check which raw SimC branch/commit the other machine actually pulled from.
#
# Lives outside data/spelldata/filtered/{class}/ on purpose: regenerate-filtered.php /
# split-by-tree.php only ever overwrite specific known filenames inside those directories and
# never wipe them, so a file placed there would technically survive too — but authoring here
# means never having to hand-write SimC's own block syntax, which these scripts control and
# this file has no reason to imitate.
#
# Read by ImportSpellData::importManualSpells(), which creates the `spells` row directly AND
# writes spell_class_availability rows with source='verified_override' immediately (skipping
# the ambiguous baseline/spec_id=NULL step entirely) — unlike an imported SimC record, there is
# no ambiguity to record here: we already know the true spec ownership because we're asserting
# it, not inferring it from incomplete source data.
#
# Format: one block per spell, BEGIN/END delimited, `key: value` lines. Required keys: spell_id
# (the real Blizzard spell id — check it against a real tooltip/addon before adding, same
# verify-first discipline as every other file in this folder), name, class (class slug), specs
# (comma-separated spec slugs, or the literal word "all" for every spec of that class).
# Optional: school, description (write real numbers directly — no $d/$sN template tokens, since
# nothing re-resolves this text), cooldown_seconds, duration_seconds, charges, mechanic (must
# match a key in ModuleSpellReferenceService::MECHANIC_CATEGORY_MAP to categorize correctly).

BEGIN
spell_id: 118
name: Polymorph
class: mage
specs: all
school: Arcane
mechanic: Polymorph
duration_seconds: 60
description: Transforms the enemy into a sheep, incapacitating them for 60 sec. Damage may interrupt the effect. Limit 1.
END

# Aimed Shot / Mirror Image, added 2026-08-13. Found investigating a real spellbook-snapshot
# diff batch (8 characters) — both showed spell_class_availability=0 (no row at all, not the
# ambiguous spec_id=NULL bucket baseline-spec-overrides.txt handles). Confirmed absent from
# SimC's own raw dump entirely, same as Polymorph — only ever referenced as a cross-reference
# target in OTHER spells' "Affecting Spells"/description text, never a "Name:" record of its
# own. cooldown_seconds/charges deliberately left blank for both — Aimed Shot's real mechanic
# is charge-based (not a flat cooldown) and no verified current-patch number is on hand; Mirror
# Image's real duration WAS directly verified (see below) so that field is filled.
#
# Aimed Shot is Marksmanship-specific, not baseline/all-spec (confirmed general game knowledge,
# consistent with every other Marksmanship-tagged spell in this same diff batch) — the real
# character it was found on is Beast Mastery, showing it only due to spec history (a real
# artifact of WoW's spellbook enumeration including any spec ever played, not the active one;
# same caveat already documented for the other 209 hits in this same diff batch).
BEGIN
spell_id: 19434
name: Aimed Shot
class: hunter
specs: marksmanship
school: Physical
description: A powerful aimed shot that deals damage.
END

# Mirror Image's duration (15 sec) is real, verified data — pulled directly from the exported
# character's own resolved in-game tooltip (spellbook_snapshot_entries.resolved_description for
# spell_id 55342), not memory or a guess. Confirmed all-spec/baseline (long-standing, stable
# Mage utility across many expansions, not spec-restricted) — the source character is Frost.
BEGIN
spell_id: 55342
name: Mirror Image
class: mage
specs: all
school: Arcane
duration_seconds: 15
description: Creates 3 copies of you nearby, which cast spells and attack your enemies. While active, you generate significantly reduced threat. Taking direct damage will cause one of your images to dissipate.
END
